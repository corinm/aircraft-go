version: v2beta1
name: aircraft

dependencies:
  aircraft-discoverer:
    path: discoverer
    pipeline: dev
  aircraft-enricher:
    path: enricher
    pipeline: dev
  aircraft-evaluator:
    path: evaluator
    pipeline: dev
  aircraft-notifier:
    path: notifier
    pipeline: dev
